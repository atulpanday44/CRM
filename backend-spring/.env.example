# ============== PostgreSQL (use with: mvn spring-boot:run -Dspring-boot.run.profiles=pg) ==============
# Create DB first: createdb internal_crm   or   ./scripts/create-db.sh
# DB_URL=jdbc:postgresql://localhost:5432/internal_crm
# DB_USERNAME=postgres
# DB_PASSWORD=your_postgres_password

# ============== H2 in-memory (use with: mvn spring-boot:run -Dspring-boot.run.profiles=dev) ==============
# No DB install; data is lost on restart.

# ============== JWT (required in production; min 32 chars) ==============
# JWT_SECRET=your-256-bit-secret-change-in-production-must-be-at-least-32-chars

# ============== First superadmin (optional; created once if none exists) ==============
# SUPERADMIN_EMAIL=admin@yourcompany.com
# SUPERADMIN_PASSWORD=your-secure-password-min-8-chars
