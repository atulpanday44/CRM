# Render Blueprint - Deploy CRM from GitHub
# Go to https://dashboard.render.com/blueprints and connect this repo

databases:
  - name: crm-db
    databaseName: internal_crm
    plan: free

services:
  # Backend (Spring Boot) - free tier (Docker supported on free tier)
  - type: web
    name: crm-backend
    plan: free
    runtime: docker
    healthCheckPath: /api
    dockerfilePath: ./backend-spring/Dockerfile
    dockerContext: ./backend-spring
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: crm-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: SUPERADMIN_EMAIL
        sync: false
      - key: SUPERADMIN_PASSWORD
        sync: false
      # Set to https://crm-frontend-XXXX.onrender.com after first deploy
      - key: CORS_ORIGINS
        sync: false

  # Frontend (static site) - always free
  - type: web
    name: crm-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      # Set to https://crm-backend-XXXX.onrender.com/api after first deploy
      - key: VITE_API_URL
        sync: false
